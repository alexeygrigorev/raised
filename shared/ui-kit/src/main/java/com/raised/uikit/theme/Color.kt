package com.raised.uikit.theme

import androidx.compose.ui.graphics.Color

// Energetic fitness palette. Work / exercise = warm "go" flame-red; rest / break
// = cool mint. The app is dark-first — the dark surfaces are the brand direction.

// Brand accents
val Flame = Color(0xFFFF5A36)
val Ember = Color(0xFFFF8A3D)
val Pulse = Color(0xFFE63946)
val Mint = Color(0xFF2EC4B6)

// Dark surfaces
val Slate = Color(0xFF0E1116)
val SlateElevated = Color(0xFF171B22)

// Content on dark
val OnSurfaceHigh = Color(0xFFF2F4F7)
val OnSurfaceMed = Color(0xFFAEB4BF)
